# Git 運用

- `main` はデプロイ専用ブランチです。通常の変更を直接コミットまたはマージしません。
- 日常の統合先は `dev` です。
- 変更を始める前に、内容が分かる `c/<変更内容>` 形式の作業ブランチを `dev` から作成します。ブランチ名は変更内容に合わせて自動的に決めます。
- 作業ブランチでは、変更に応じた lint・型検査・テスト・ビルドを実行します。
- 検証に成功した作業ブランチだけを、マージコミットを残して `dev` へマージします。
- `main` への反映は、デプロイを明示的に実施するときだけです。
- 未コミットの利用者変更を上書きせず、必要な場合は先に専用ブランチへ保護します。
